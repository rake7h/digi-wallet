import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1, // Ensure the container takes up 100% of the width
  },
  cardContainer: {
    padding: 16,
  },
  cardText: {
    fontSize: 16,
    marginBottom: 8,
  },
});
