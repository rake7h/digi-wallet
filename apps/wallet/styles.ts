import { StyleSheet } from "react-native";
import { theme } from "@digi-wallet/theme";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: theme.dark.colors.darkBlack,
  },
});
